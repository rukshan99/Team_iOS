<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.getsettravel" >
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".AdminDashBoard" android:theme="@style/AdminTheme" />
        <activity android:name=".BookingList" />
        <activity android:name=".Payment_confirm" />
        <activity android:name=".PasswordReset" />
        <activity android:name=".UserAccount" />
        <activity android:name=".Registration" />
        <activity android:name=".BookingConfirm" />
        <activity android:name=".PaymentDetails" />
        <activity android:name=".AddHotelDetails" />
        <activity android:name=".AboutUs" />
        <activity android:name=".Login" />
        <activity android:name=".EditDetails" />
        <activity android:name=".UserList" android:theme="@style/AdminTheme" />
        <activity android:name=".ResetPassword" />
        <activity android:name=".AddHotel" android:theme="@style/AdminTheme"/>
        <activity android:name=".HotelList" android:theme="@style/AdminTheme"/>
        <activity android:name=".UpdateHotel" android:theme="@style/AdminTheme"/>
        <activity android:name=".HotelMoreInfo" android:theme="@style/AdminTheme"/>



        <activity android:name=".Index" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ContactUs" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/AIzaSyDBVxHhz8sTb4s8TXUvh5wUBxs4Tk_JgEk" />

        <activity
            android:name=".Map"
            android:label="@string/title_activity_map" />
        <activity android:name=".Amount" />
        <activity android:name=".BookingDetails" />
        <activity android:name=".home" />
        <activity android:name=".Feedback" />
        <activity android:name=".AdminBookingList" android:theme="@style/AdminTheme" />
        <activity android:name=".FeedbackDetails" android:theme="@style/AdminTheme" />
        <activity android:name=".PromotionList" android:theme="@style/AdminTheme" />
        <activity android:name=".AddPromo" android:theme="@style/AdminTheme" />
        <activity android:name=".UpdatePromo" android:theme="@style/AdminTheme"  />


    </application>

</manifest>